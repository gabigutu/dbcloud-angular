import { WriteascheckPipe } from './writeascheck.pipe';

describe('WriteascheckPipe', () => {
  it('create an instance', () => {
    const pipe = new WriteascheckPipe();
    expect(pipe).toBeTruthy();
  });
});
