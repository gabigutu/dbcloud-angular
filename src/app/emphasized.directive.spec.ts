import { EmphasizedDirective } from './emphasized.directive';

describe('EmphasizedDirective', () => {
  it('should create an instance', () => {
    const directive = new EmphasizedDirective();
    expect(directive).toBeTruthy();
  });
});
